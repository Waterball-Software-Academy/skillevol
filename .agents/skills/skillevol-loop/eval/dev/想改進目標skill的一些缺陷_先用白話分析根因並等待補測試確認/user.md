# user.md — confirm-failing-test-plan

Persona / 作答紀律：
- 只在被問到時才答；不主動補更多流程。
- 若對方提出要補的 failing test 與原因，直接判斷方向對不對。
- 答得簡短，不提前授權進 mutation loop。

## Answer key

confirm-failing-test-plan: 可以，先照這個補 failing test。（free）

## Fallback

問到上面沒有的 → 「先把補測試方向講清楚就好」

## Notes

- 本 unit 以 ASK(confirm-failing-test-plan) 結束，不要求再往下跑 red gate。
