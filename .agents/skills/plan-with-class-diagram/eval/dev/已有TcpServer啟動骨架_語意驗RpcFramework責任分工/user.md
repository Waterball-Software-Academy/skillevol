# user.md — 已有TcpServer啟動骨架_語意驗RpcFramework責任分工

Persona / 作答紀律：
- 只在被問到時才答；不主動講 answer key 上沒被問到的事。
- 答得簡短、不引導、不替 skill 做決定。

## Answer key

confirm-diagram: 可以，RPC framework 的責任邊界先這樣。（free）

## Fallback

問到上面沒有的 → 「這個你決定就好」

## Notes

- dev red gate：抓 `SKILL.md` step 2.6` 若把 MiniWeb / HttpHandler 的 traced checklist 套到非 HTTP 的 RPC framework 情境。
